# Model Context Protocol（MCP）

## 概要
- LLM（AI Agent）が外部ツールを呼び出す際の仕様を標準化するプロトコル
- ツール呼び出しの形式・レスポンス・エラー処理を統一する
- Bedrock専用ではなく、ツール連携全般に適用される設計思想

---

## 役割

- ツール呼び出しの標準化
- 入出力フォーマットの統一
- エラー処理の一貫化
- リトライ制御の実現

---

## なぜ必要か

### MCPなし
- ツールごとに呼び出し形式が異なる
- エラー形式がバラバラ
- 再試行ロジックが複雑

### MCPあり
- 統一されたインターフェース
- 安定したツール連携
- スケーラブルな設計

---

## アーキテクチャ

LLM（Agent）
↓
MCP（呼び出し仕様）
↓
ツール（Lambda / API）
↓
結果（統一フォーマットで返却）

---

## 実装例（AWS）

- MCP：ツール呼び出し仕様の定義
- AWS Lambda：ツールの実装
- API Gateway：エンドポイント（必要に応じて）

---

## メリット

### ① 呼び出しの統一
- どのツールでも同じ形式で呼び出し可能

### ② エラー処理の統一
- 例外を一貫した形式で返却

### ③ リトライ制御
- エラー時の再試行が容易

### ④ 拡張性
- ツール追加しても設計が崩れない

---

## Bedrockとの関係

- MCPはBedrock専用ではない
- Bedrock Agentsと相性が良い
- Agentのツール呼び出し設計の基盤となる

---

## 関連概念

### Agent
- 推論し、ツールを選択・実行する主体

### MCP
- ツール呼び出しのルール

### Lambda
- 実際の処理を行うツール実装

---

## 試験ポイント

- 「ツール呼び出しの標準化」→ MCP
- 「エラー処理・リトライ制御」→ MCP + Lambda
- Agent単体ではなく、プロトコルとしてMCPが必要

---

## よくある誤り

- Lambdaだけで対応 → NG（統一されない）
- Agentだけで完結 → NG（仕様不足）
- MCPをBedrock専用と誤解 → NG

---

## 一言まとめ

- MCP = LLMがツールを安全・統一的に呼び出すための共通ルール

---

# ReAct（Reason + Act）

## 概要
- ReAct = Reason（推論）+ Act（行動）
- 推論と外部アクションを交互に実行する設計パターン
- Agent的なワークフローで使用される

---

## 基本構造

Reason（考える）  
↓  
Act（外部ツール呼び出し）  
↓  
Observation（結果取得）  
↓  
Reason（再判断）  
↓  
...（繰り返し）  

---

## 特徴

- 単発の推論ではなく、複数ステップで処理
- 外部APIやツールを活用できる
- 状況に応じて次の行動を動的に決定

---

## 例（配送ルート最適化）

1. Reason：交通情報が必要
2. Act：交通API呼び出し
3. Observation：渋滞情報取得
4. Reason：コスト計算が必要
5. Act：コストAPI呼び出し
6. Observation：コスト取得
7. Reason：最適ルート決定
8. Answer：結果出力

---

## 利用シーン

- 外部API連携が必要な処理
- 段階的な意思決定が必要なタスク
- 不確実な情報を補完しながら処理する場合
- Agentベースのアプリケーション

---

## AWSでの実装

### Step Functionsとの組み合わせ

- 推論とアクションを状態遷移として管理
- エラー処理・リトライ制御が可能
- ワークフローの可視化・保守性向上

---

## 試験ポイント

- 「推論とアクションを交互に実行」→ ReAct
- 「外部APIを途中で呼び出す」→ ReAct
- 「複数ステップの意思決定」→ ReAct
- 「Agent的処理」→ ReAct

---

## 他手法との違い

### Prompt Chaining
- 手順が固定
- 動的な判断は少ない

### ReAct
- 状況に応じて次の行動を決定
- より柔軟で動的

---

## よくある誤り

- 単なるワークフローと混同 → NG
- 1回の推論で完結する処理 → NG
- 外部ツールを使わない → NG

---

## 一言まとめ

- ReAct = 「考えて → 行動して → また考える」を繰り返すパターン

---

# エージェントの不要なツール呼び出し削減

## 概要
- エージェントは外部ツール（API等）を呼び出してタスクを解決する
- 不要なツール呼び出しはコスト・遅延・不安定性を増加させる
- 回答精度だけでなく「行動効率」の評価が重要

---

## 不要なツール呼び出しの例

- 同じツールを同じ条件で繰り返し実行
- 本来不要なツールを呼び出す
- 順序が不適切（検証前に実行など）
- 無関係なツールの利用
- 過剰な確認処理

---

## 発生原因

- ツール説明が曖昧
- 状態管理不足（キャッシュなし）
- 終了条件が不明確
- エージェント間の責務重複
- ルーティング精度不足

---

## 評価指標（重要）

### 基本指標
- tool_calls_per_task（タスクあたり呼び出し数）
- redundant_call_rate（冗長呼び出し率）
- useful_call_ratio（有効呼び出し率）
- successful_tool_use_rate（成功率）

### 応用指標
- average_steps_to_resolution（解決までのステップ数）
- unnecessary_action_rate（不要アクション率）
- repeated_observation_rate（再取得率）

---

## 分析観点

- 呼び出しが本当に必要だったか
- 順序は適切か
- 責務分担は適切か
- 停止条件は守られているか

---

## 削減のための対策

### 1. ツール仕様の明確化
- 使用条件・対象範囲を明示

### 2. プロンプト改善
- 重複呼び出し禁止
- 必要な場合のみ実行

### 3. キャッシュ導入
- 同一リクエストの再利用

### 4. エージェント分離
- 役割を明確に分割

### 5. ルーティング改善
- 最初の分岐精度を向上

### 6. 終了条件の明示
- 「いつ終わるか」を定義

---

## マルチエージェント特有の問題

- 同じ情報の再取得
- 不要な委譲
- エージェント間の往復増加

### 対策
- コンテキスト共有
- サブタスク入力の明確化
- 再検索禁止

---

## 評価方法

### オフライン評価
- ゴールデンタスクで検証
- 正答率 + 呼び出し効率を測定

### オンライン評価
- ログ分析
- コスト・レイテンシ監視

---

## 試験ポイント

- 「不要なツール呼び出し」→ 行動ログ分析
- 「エージェント最適化」→ 指標設計
- 「品質評価」→ 精度 + 効率

---

## 一言まとめ

- エージェント最適化 = 行動ログを分析し、無駄なツール呼び出しを削減すること

---

# AWS Glue におけるスキーマドリフト検出と監視

## 概要
- スキーマドリフト = 入力データの構造（カラム・型）が変化すること
- Glueでは Crawler + EventBridge + CloudWatch を組み合わせて検知・監視する

---

## スキーマドリフト検出

### Glue Crawler

#### 役割
- データソースをスキャン
- スキーマを自動推論
- Data Catalog を更新

#### ポイント
- カラム追加・削除・型変更を検知
- 定期実行でドリフトを把握

---

## Data Catalog

- スキーマの中央管理
- Crawlerが更新するメタデータストア
- スキーマ変更の履歴としても利用可能

---

## イベント通知（EventBridge）

### 主なイベント

- Glue Crawler State Change
- Glue Job State Change
- Data Catalog Table Update

---

## 典型的な構成

Crawler実行  
↓  
スキーマ変更検出  
↓  
Data Catalog更新  
↓  
EventBridgeイベント発火  
↓  
SNS / Lambda / Step Functions  

---

## 通知・自動処理

### SNS
- スキーマ変更を通知

### Lambda
- スキーマ差分チェック
- 自動補正処理

### Step Functions
- 再処理フロー起動

---

## ETLジョブの健全性監視

### Glue Job State Change

- SUCCEEDED
- FAILED
- TIMEOUT

→ EventBridgeで検知

---

### CloudWatch

- メトリクス監視
- ログ分析
- アラーム設定

---

## 試験ポイント

- 「スキーマドリフト」→ Glue Crawler
- 「スキーマ自動検出」→ Data Catalog
- 「イベント通知」→ EventBridge
- 「ジョブ監視」→ CloudWatch

---

## よくある誤り

- ETLジョブがスキーマ検出 → NG（Crawler）
- 手動スキーマ管理 → NG
- EventBridgeなしで監視 → NG

---

## 一言まとめ

- Crawler = スキーマ検出
- EventBridge = 変化の通知
- CloudWatch = ジョブ監視

---

# Amazon Q Developer CLI と MCP（Model Context Protocol）

## 概要
- Q Developer CLI = コマンドラインから使えるAI開発アシスタント
- MCP = AIが外部データソースへリアルタイムアクセスする仕組み
- 組み合わせることで「動的コンテキスト取得」が可能

---

## Amazon Q Developer CLI

### 役割
- ターミナルからAIアシスタントを利用
- 開発・運用作業を支援

### 主な機能
- コード生成・修正
- コマンド提案
- トラブルシュート
- ローカルファイル参照
- CLI操作支援

---

## MCP（Model Context Protocol）

### 役割
- AIが外部データをリアルタイム取得するためのプロトコル

### 接続可能なデータソース
- データベース（RDS / DynamoDBなど）
- 外部API
- 社内サービス
- SaaSツール

---

## Q Developer CLI × MCP

### できること
- 会話中にリアルタイムデータ取得
- 動的なコンテキスト付与
- 最新情報に基づく回答生成

---

## アーキテクチャイメージ

ユーザー  
↓  
Q Developer CLI  
↓  
MCP  
↓  
外部データソース（DB / API）  
↓  
リアルタイム取得  
↓  
回答生成  

---

## RAGとの違い

### RAG
- 事前にデータを埋め込み
- ベクトル検索で取得

```text
静的データ
```

---

### MCP
- 必要なときに直接取得

```text
動的データ（リアルタイム）
```

---

## 使い分け

- 最新データ・リアルタイム性 → MCP
- 大量文書・検索 → RAG

---

## 試験ポイント

- 「リアルタイムデータ」→ MCP
- 「外部API連携」→ MCP
- 「動的コンテキスト」→ MCP + CLI
- 「Q Developerで外部接続」→ CLI

---

## 一言まとめ

- Q Developer CLI + MCP = AIがリアルタイムで外部データを参照できる仕組み
