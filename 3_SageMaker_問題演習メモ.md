# SageMaker の非同期エンドポイント

## 正解キーワード
- 非同期エンドポイント（Asynchronous Inference Endpoint）
- 長時間推論
- キューイング
- タイムアウト回避
- 高負荷時の安定運用
- 即時受付応答
- 後続で結果取得

---

## 解法パターン

### 問題の状況
- 画像生成など**推論時間が長い**
- リクエスト急増で**フロントエンドがタイムアウト**
- 高負荷時でも**安定した処理継続**が必要

### 判断
この条件なら  
→ **SageMaker 非同期エンドポイント**

---

## なぜこれが正解か

- 非同期エンドポイントは、推論リクエストを**キューに積める**
- クライアントには**すぐ受付応答**を返せる
- 実際の推論処理はバックグラウンドで実行される
- 長時間処理でもHTTPタイムアウトを避けやすい
- バースト時も吸収しやすく、生成系ワークロードと相性がよい

---

## 試験での判断ポイント

### こう書いてあれば非同期を疑う
- 推論に時間がかかる
- タイムアウトが発生している
- 即時レスポンスは不要
- 処理完了後に結果取得できればよい
- バーストアクセスに耐えたい

👉 これらが出たら  
→ **非同期エンドポイント**

---

## よくある誤答パターン

### リアルタイムエンドポイント
→ ❌ 即時応答向け  
→ 長時間推論やピーク時タイムアウトには不利

### バッチ変換（Batch Transform）
→ ❌ オフライン一括処理向け  
→ リクエスト駆動の生成APIには不向き

### エンドポイントのインスタンス増強だけ
→ ❌ 一時的改善にはなるが、本質的に長時間リクエスト問題は残る

---

## 試験対策まとめ

- 長時間推論 → 非同期エンドポイント
- タイムアウト回避 → 非同期
- 即時API応答が必要だが結果は後でよい → 非同期
- 一括オフライン処理 → Batch Transform

最重要：
「長時間推論 + 高負荷 + タイムアウト」なら **非同期エンドポイント**

# SageMaker Pipelines / Model Registry

## 概要

### SageMaker Pipelines
- 機械学習ワークフロー（前処理〜学習〜評価〜モデル生成）を自動化するサービス
- 再現性のあるMLパイプラインを構築できる
- MLにおけるCI/CDの役割

### SageMaker Model Registry
- モデルのバージョン・状態・履歴を管理するサービス
- モデルのライフサイクル管理を担う

---

## SageMaker Pipelines

### 役割
- データ前処理
- 学習
- 評価
- モデル成果物の生成
- 条件分岐（精度によるデプロイ可否判断など）

### 特徴
- 毎回「新しいモデル」を生成する
- 既存モデルは上書きしない（immutable）
- 再現性を担保できる

### 重要ポイント
- 同じ入力 → 同じ出力（再現性）
- モデルは更新しない、増やす

---

## SageMaker Model Registry

### 役割
- モデルのバージョン管理
- 承認ステータス管理（Approved / Pending / Rejected）
- デプロイ対象の管理
- ロールバック対応

### 管理対象
- モデル成果物
- 学習データ情報
- ハイパーパラメータ
- 評価結果
- lineage（系譜）

---

## モデル管理のベストプラクティス

- モデルはimmutable（上書きしない）
- バージョン単位で管理
- lineage（由来）を追跡可能にする

### イメージ

Model A（データv1）
↓
Model B（データv2）
↓
Model C（パラメータ変更）

---

## ロールバック

- 問題発生時は過去バージョンを再デプロイ
- Model Registryで管理されたバージョンを切り替える

---

## PipelinesとRegistryの関係

- Pipelines：モデルを作る
- Registry：モデルを管理する

---

## 試験ポイント

- 再現性 → SageMaker Pipelines
- バージョン管理 → Model Registry
- ロールバック → Model Registry
- immutable（不変） → モデルは上書きしない

---

## よくある誤り

- モデルを上書き更新する → NG
- Pipelineがロールバックする → NG
- Model Registryは保存だけの機能 → NG

---

## 一言まとめ

- Pipelines = 作る（再現性）
- Registry = 管理する（履歴・承認・ロールバック）

---

# SageMaker Model Registry とロールバック

## 概要
- SageMakerではモデルをModel Registryでバージョン管理する
- 問題発生時は安定版モデルへロールバックする
- ロールバックは「モデル選定」と「Endpoint更新」の2ステップ

---

## Model Registry

### 役割
- モデルのバージョン管理
- 承認状態（Approved / Pending / Rejected）管理
- メタデータ（評価指標・パラメータ）の管理

### 中身
- モデル成果物（S3）
- 推論コンテナ（ECR）
- 設定情報

---

## ロールバックの流れ

1. 問題発生（品質低下など）
2. Model Registryで安定版モデルを特定
3. 安定版モデルのARNを取得
4. EndpointをそのARNで更新
5. ロールバック完了

---

## 重要ポイント

### ① モデルの特定（分析）
- どのバージョンが正常だったか確認
- Approvedモデルを参照

### ② Endpoint更新（実行）
- ARNを指定して更新
- 裏でデプロイが実行される

---

## Endpoint更新の実態

- 新しいモデルを指定
- SageMakerが以下を自動実行
  - コンテナ起動
  - モデルロード
  - トラフィック切り替え

※ 「デプロイ」と「切り替え」は一体の操作

---

## アーキテクチャ

Model Registry
↓
Model（ARN）
↓
Endpoint Configuration
↓
Endpoint（実行環境）

---

## 試験ポイント

- Model Registry = モデル管理
- ARN指定 = モデル選択
- Endpoint更新 = デプロイ + 切り替え
- ロールバック = 安定版モデルのARNに切り替える

---

## よくある誤り

- Model Registryだけでロールバックできる → NG
- デプロイと切り替えを別操作と考える → NG
- モデルを再学習する必要がある → NG

---

## 一言まとめ

- Model Registryで選び、Endpoint更新で戻す
