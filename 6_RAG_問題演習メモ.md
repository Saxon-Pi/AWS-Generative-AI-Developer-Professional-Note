## 目次
- [RAGにおけるタグ / メタデータ / フィルタリング](#ragにおけるタグ--メタデータ--フィルタリング)
- [多言語EmbeddingとVector Store設計（RAG）](#多言語embeddingとvector-store設計rag)
- [チャンク戦略まとめ（RAG / Bedrock Knowledge Bases）](#チャンク戦略まとめrag--bedrock-knowledge-bases)
- [文脈整合性チェック（RAG）](#文脈整合性チェックrag)
- [RAGにおけるチャンク重複とMMR（多様性）](#ragにおけるチャンク重複とmmr多様性)
- [GraphRAG（ナレッジグラフ × RAG）](#graphragナレッジグラフ--rag)

---

# RAGにおけるタグ / メタデータ / フィルタリング

## 概要
- RAGの検索精度は、ベクトル検索だけでなくタグやメタデータによる絞り込みでも改善できる
- セマンティック検索とメタデータ検索は役割が異なる
- 実務では両方を組み合わせる

---

## セマンティック検索

### 役割
- 意味の近さで文書を検索する
- Queryと文書をEmbeddingし、ベクトル距離で類似度を計算する

### 強み
- 言い換えに強い
- 自然言語の質問に対応しやすい

### 弱み
- カテゴリ違いの文書も混ざることがある
- 著者名、ID、日付、カテゴリなどの厳密条件には弱い

---

## タグ / メタデータ検索

### 役割
- 構造化情報で検索対象を絞り込む
- セマンティック検索の前段または後段で使う

### 例
- topic = sports
- author = Tanaka
- date = 2025-01-01

### 使い所
- カテゴリで絞る
- 著者で絞る
- 公開日や言語で絞る
- テナントやアクセス権で絞る

---

## タグとメタデータの違い

### タグ
- key=value 形式
- シンプルな分類に向く
- 例: topic=sports

### メタデータ
- 文書に付随する追加属性
- より柔軟に検索条件として使える
- 例: author=Tanaka, source=news, language=ja

---

## 検索の組み合わせ

### 基本パターン
1. タグ / メタデータで絞る
2. 絞られた文書集合に対してセマンティック検索する

### 例
- topic = sports で絞る
- その中から「サッカー戦術」に意味的に近い記事を探す

---

## なぜ精度が上がるか

### セマンティック検索だけの場合
- 関係ないカテゴリの記事が混ざる
- ノイズが増える

### フィルタを使う場合
- 検索対象が狭くなる
- ノイズが減る
- ベクトル検索の精度が上がる

---

## RAGパイプラインでの位置づけ

User Query
↓
フィルタ（タグ / メタデータ）
↓
Vector Search
↓
Top-K取得
↓
LLM

---

## ハイブリッド検索との関係

- セマンティック検索 = 意味検索
- メタデータ検索 = 条件フィルタ
- BM25などのキーワード検索も加えると、さらに精度を上げられる

### 整理
- メタデータ = 絞る
- セマンティック = 探す
- キーワード = 単語一致で補完する

---

## 試験ポイント

- 「カテゴリ」「著者」「日付」「アクセス権」などがある場合は、メタデータを活用する
- セマンティック検索だけでは厳密条件に弱い
- RAG精度改善では、メタデータフィルタがよく正解になる
- 「タグを付けて検索で活用」は典型パターン

---

## よくある誤り

- ベクトル検索だけで十分と考える
- メタデータを保存しても検索に使わない
- 厳密条件までセマンティック検索で解決しようとする

---

## 一言まとめ

- メタデータ = 絞る
- セマンティック検索 = 探す
- 両方を組み合わせるとRAG精度が上がる

---

# 多言語EmbeddingとVector Store設計（RAG）

## 概要
- 多言語Embeddingを使うと、言語が異なっても「意味」が近い文書は近いベクトルになる
- 基本は1つのVector Storeにまとめ、メタデータで制御する

---

## 多言語Embeddingの性質

- 単語/文 → ベクトル（意味空間の座標）
- 同義語は言語を超えて近くなる

例
- 「旅行」≒ "trip" ≒ "travel" ≒ 「観光」

---

## 前提条件

- Embeddingモデルが多言語対応であること
- 英語特化モデルではクロス言語の精度が落ちる

---

## RAGでの挙動

ユーザクエリ（任意言語）  
↓  
Embedding  
↓  
ベクトル検索（ANN / Top-K）  
↓  
多言語ドキュメントがヒット（意味が近い順）  

---

## 重要ポイント

### ① 意味ベース検索
- 言語ではなく意味で近くなる

### ② 完全一致ではない
- ニュアンス差・学習データによりズレる

### ③ モデル依存
- 多言語対応モデルであることが前提

---

## 設計パターン（推奨）

### 1つのVector Store + メタデータ

例
```json
{
  "text": "...",
  "language": "ja"
}
```

検索時
- ベクトル検索（意味）
- メタデータフィルタ（言語）

---

## いつ分けるべきか

- Embeddingが単一言語特化
- 言語ごとに厳密な出力制御が必要
- ノイズを減らしたい場合

---

## ベストプラクティス

- 基本は1つのVector Storeに統合
- メタデータで言語・カテゴリを制御
- 必要に応じてフィルタリング

---

## 試験ポイント

- 多言語対応 → 1つのVector StoreでOK
- 前提 → 多言語Embedding
- 精度向上 → メタデータフィルタ併用

---

## よくある誤り

- 言語ごとに必ず分割 → NG
- 常に同じ言語だけ返る → NG
- Embeddingの種類を無視 → NG

---

## 一言まとめ

- 多言語Embeddingなら「意味」でつながる
- Vector Storeは統合、制御はメタデータ

---

# チャンク戦略まとめ（RAG / Bedrock Knowledge Bases）

## 概要
- チャンク = 文書を分割した単位
- RAGの検索精度に大きく影響する重要要素
- 「どう分割するか」が品質を左右する

---

## 基本概念

- 文書をそのままEmbeddingすると長すぎる
- → 小さく分割（チャンク化）して管理

---

## チャンク戦略の種類

### 1. 固定長チャンク

#### 定義
- 文字数やトークン数で機械的に分割

#### 特徴
- 実装が簡単
- 意味が途中で切れる

#### 用途
- シンプルなデータ
- 前処理コストを抑えたい場合

---

### 2. 階層型チャンク

#### 定義
- 文書構造（章・節・段落）に基づいて分割

#### 特徴
- 意味のまとまりを保てる
- 検索精度が高い

#### 例
章 → 節 → 段落 のような特定の単位でチャンク化が可能  
→ 段落単位でチャンク化する（選択）  

※ 明示的に指定するのではなく、  
　 システムが「文章の意味を保持するのに適切な単位」を選択してくれる

#### ポイント
- 「構造ベース」で分割
- 1つの粒度で管理（複数バージョンではない）

---

### 3. オーバーラップチャンク

#### 定義
- 隣接チャンクに一部重複を持たせる

#### 例
チャンク1：[A B C]
チャンク2：[B C D]

#### 特徴
- 文脈切れを防ぐ
- トークンコスト増加

---

### 4. マルチ粒度チャンク（上級）

#### 定義
- 複数の粒度（粗い/細かい）でチャンクを持つ

#### 特徴
- 検索精度向上
- 実装が複雑

#### 注意
- 階層型チャンクとは別概念

---

## 比較

|戦略|特徴|用途|
|--|--|--|
固定長|単純|低コスト|
階層型|意味保持|標準/推奨|
オーバーラップ|文脈保持|精度向上|
マルチ粒度|高度検索|上級設計|

---

## 設計ポイント

- 意味のまとまりを優先
- チャンクサイズは小さすぎても大きすぎてもNG
- 必要に応じてオーバーラップを追加
- メタデータと組み合わせると精度向上

---

## 試験ポイント

- 「意味を壊さない」→ 階層型チャンク
- 「長文で途切れる」→ オーバーラップ
- 「シンプル」→ 固定長
- 「高度な検索」→ マルチ粒度

---

## よくある誤り

- 固定長で十分 → NG
- 階層型 = 自動最適分割 → NG
- 階層型 = 複数粒度 → NG

---

## 一言まとめ

- チャンク設計 = RAGの精度を決める重要要素
- 基本は「階層型 + オーバーラップ」

---

# 文脈整合性チェック（RAG）

## 概要
- 文脈整合性チェック = 検索結果が質問意図に適合しているかを判定する処理
- RAGにおける精度向上の重要パターン
- 特定のAWS機能ではなく設計パターン

---

## 背景

### 問題
- ベクトル検索は「意味が近い」文書を取得する
- しかし「質問に対して適切」とは限らない

### 結果
- 無関係な文書が混入
- 誤回答・ハルシネーションの原因

---

## RAGの処理フロー

検索（Retrieve）
↓
文脈整合性チェック ← ★追加
↓
生成（Generate）

---

## 文脈整合性チェックの役割

- 質問と文書の関連性を判定
- 不適切な文書を除外
- 高品質なコンテキストのみを生成に使用

---

## 実装パターン

### 1. LLMによる判定（推奨）
- 質問 + 文書を入力
- 「この文書は質問に答えるのに適切か？」を判定
- YES/NOでフィルタリング

---

### 2. スコアリング
- 類似度スコア + 閾値
- 条件に応じて除外

---

### 3. クロスエンコーダ（上級）
- クエリと文書を同時に評価
- より高精度な関連性判定

---

## Bedrockとの関係

- 専用機能としては存在しない
- LLM（Claudeなど）を使って実装可能

例：
- Bedrockに「この文書は質問に関連しているか？」と判定させる

---

## Knowledge Basesとの違い

- Knowledge Bases：検索・再ランキングまで
- 文脈整合性チェック：意図レベルの適合性判定

---

## 利用シーン

- RAGの精度改善
- 医療・金融など高精度が求められる領域
- ハルシネーション抑制

---

## 試験ポイント

- 「検索結果がズレる」→ 文脈整合性チェック
- 「RAG精度改善」→ フィルタリング追加
- 「意図に合わない文書」→ 除外

---

## よくある誤り

- ベクトル検索だけで十分 → NG
- モデルサイズ拡大で解決 → NG
- 再ランキングだけで解決 → NG

---

## 一言まとめ

- 文脈整合性チェック = 検索結果が質問に適切かを判定してフィルタする仕組み

---

# RAGにおけるチャンク重複とMMR（多様性）

## 概要
RAGシステムでは、チャンク分割時にオーバーラップ（重複）を持たせることで文脈を維持するが、
その副作用として「似たチャンクが検索結果に集中する問題」が発生する。

---

## 一言で

オーバーラップ = 文脈維持のために必要  
ただし → 類似チャンクの塊が発生する

---

## チャンク分割（オーバーラップ）

例：

chunk1: hoge hoge fuga fuga X Y Z  
chunk2:      fuga fuga X Y Z A  
chunk3:           fuga X Y Z A B  

👉 少しずつズラして分割

---

## なぜオーバーラップする？

- 文脈が途切れないようにするため
- LLMが理解しやすくするため

---

## 問題（副作用）

### ① 類似チャンクが大量生成
- 同じ意味の文章が複数存在

### ② 検索結果が偏る
- Top-Kが同一トピックで埋まる

### ③ 多様性が低下
- 情報の幅が狭くなる

### ④ ハルシネーション増加
- 情報不足をLLMが補完する

---

## 検索時の挙動

ベクトル検索：

query embedding  
 ↓  
類似度計算（cosine similarity）  
 ↓  
Top-K取得  

👉 問題：  
「似ているもの」だけを取得するため、
同じ内容のチャンクが複数ヒットする

---

## 解決策：MMR（Maximum Marginal Relevance）

### 一言で

MMR = 関連性 + 多様性 を両立するアルゴリズム

---

## MMRの考え方

① 最初は一番関連性が高いものを選ぶ  
② 次は：
- クエリに近い
- かつ既存結果と似ていない

ものを選ぶ

---

## スコアのイメージ
```
Score = λ × クエリ類似度  
      - (1-λ) × 既存結果との類似度
```
---

## 効果

- 類似チャンクの重複排除
- 情報の多様性向上
- コンテキストの充実
- ハルシネーション低減

---

## 具体例

### k-NN（問題あり）

1. IAMポリシー  
2. IAMロール  
3. IAM設定  
4. IAM例  
5. IAMベストプラクティス  

👉 偏り

---

### MMR（改善）

1. IAMポリシー  
2. VPCセキュリティ  
3. CloudTrail  
4. KMS  
5. GuardDuty  

👉 多様性あり

---

## その他の対策

### ① chunk overlap調整
- 重複を減らす

### ② re-ranking
- 後処理で多様性を調整

### ③ parent-child chunking
- 文書単位でバランス制御

---

## 試験ポイント

- 類似チャンクが集中 → MMR
- 多様性不足 → MMR
- ハルシネーション増加 → MMRで改善

---

## 一言まとめ

オーバーラップチャンクは文脈維持に重要だが、
MMRで多様性を確保しないと検索結果が偏る

---

# GraphRAG（ナレッジグラフ × RAG）

## 概要
GraphRAGは、従来のベクトル検索ベースのRAGにナレッジグラフを組み合わせ、
「意味」と「関係性」の両方を扱えるようにした高度なRAGアーキテクチャ。

---

## 一言で

GraphRAG = ベクトル検索（意味） + ナレッジグラフ（関係）

---

## なぜ必要か

### 従来RAGの限界

- 意味的に近い文章は取得できる
- しかしエンティティ間の関係性を理解できない

---

## 問題例

「A社とB社の間の仲介企業は？」

### 従来RAG
- A社の情報
- B社の情報
→ 関係を正しく結合できない

---

## ナレッジグラフとは

### 構造

- ノード（エンティティ）
- エッジ（関係）

例：

[A社] --取引--> [C社] --取引--> [B社]

---

## GraphRAGの仕組み

### ① データ準備

- ドキュメントからエンティティ抽出
- 関係抽出
- グラフ構築（Neptuneなど）

---

### ② クエリ処理

ユーザー質問
 ↓
グラフ検索（関係探索）
 ↓
関連ノード・パス取得
 ↓
RAGで回答生成

---

## ベクトルRAGとの違い

### 従来RAG
- 意味的類似性ベース

### GraphRAG
- 意味 + 関係性 + 構造

---

## メリット

### ① 複雑な関係を扱える
- サプライチェーン
- 組織構造
- 人物相関

---

### ② ハルシネーション低減
- 構造的根拠に基づく回答

---

### ③ 精度向上
- 推論ではなく探索ベース

---

## デメリット

### ❌ 構築コストが高い
- エンティティ抽出
- 関係抽出
- グラフ構築

---

### ❌ 更新が複雑
- 関係の維持が必要

---

## AWS構成例

S3（ドキュメント）  
 ↓  
Bedrock / Comprehend（抽出）  
 ↓  
Neptune（グラフDB）  
 ↓  
OpenSearch（ベクトルDB）  
 ↓  
Bedrock（生成）  

---

## 役割分担

- Neptune：関係性の管理
- Vector DB：意味検索
- LLM：回答生成

---

## 試験ポイント

以下のキーワードでGraphRAG：

- 関係性
- 仲介
- ネットワーク
- 多段推論
- 相関関係

---

## 一言まとめ

GraphRAG = 関係性を扱えるようにした進化版RAG
