# RAGにおけるタグ / メタデータ / フィルタリング

## 概要
- RAGの検索精度は、ベクトル検索だけでなくタグやメタデータによる絞り込みでも改善できる
- セマンティック検索とメタデータ検索は役割が異なる
- 実務では両方を組み合わせる

---

## セマンティック検索

### 役割
- 意味の近さで文書を検索する
- Queryと文書をEmbeddingし、ベクトル距離で類似度を計算する

### 強み
- 言い換えに強い
- 自然言語の質問に対応しやすい

### 弱み
- カテゴリ違いの文書も混ざることがある
- 著者名、ID、日付、カテゴリなどの厳密条件には弱い

---

## タグ / メタデータ検索

### 役割
- 構造化情報で検索対象を絞り込む
- セマンティック検索の前段または後段で使う

### 例
- topic = sports
- author = Tanaka
- date = 2025-01-01

### 使い所
- カテゴリで絞る
- 著者で絞る
- 公開日や言語で絞る
- テナントやアクセス権で絞る

---

## タグとメタデータの違い

### タグ
- key=value 形式
- シンプルな分類に向く
- 例: topic=sports

### メタデータ
- 文書に付随する追加属性
- より柔軟に検索条件として使える
- 例: author=Tanaka, source=news, language=ja

---

## 検索の組み合わせ

### 基本パターン
1. タグ / メタデータで絞る
2. 絞られた文書集合に対してセマンティック検索する

### 例
- topic = sports で絞る
- その中から「サッカー戦術」に意味的に近い記事を探す

---

## なぜ精度が上がるか

### セマンティック検索だけの場合
- 関係ないカテゴリの記事が混ざる
- ノイズが増える

### フィルタを使う場合
- 検索対象が狭くなる
- ノイズが減る
- ベクトル検索の精度が上がる

---

## RAGパイプラインでの位置づけ

User Query
↓
フィルタ（タグ / メタデータ）
↓
Vector Search
↓
Top-K取得
↓
LLM

---

## ハイブリッド検索との関係

- セマンティック検索 = 意味検索
- メタデータ検索 = 条件フィルタ
- BM25などのキーワード検索も加えると、さらに精度を上げられる

### 整理
- メタデータ = 絞る
- セマンティック = 探す
- キーワード = 単語一致で補完する

---

## 試験ポイント

- 「カテゴリ」「著者」「日付」「アクセス権」などがある場合は、メタデータを活用する
- セマンティック検索だけでは厳密条件に弱い
- RAG精度改善では、メタデータフィルタがよく正解になる
- 「タグを付けて検索で活用」は典型パターン

---

## よくある誤り

- ベクトル検索だけで十分と考える
- メタデータを保存しても検索に使わない
- 厳密条件までセマンティック検索で解決しようとする

---

## 一言まとめ

- メタデータ = 絞る
- セマンティック検索 = 探す
- 両方を組み合わせるとRAG精度が上がる
